# V15 stable deployment

Branch staging for deterministic V15.0.1 runtime deployment. Runtime snapshot is checksum-validated before any program files are materialized. The local `data/` directory is never overwritten by the V15 bootstrap.
